# Export templates

## Template.pptx

Consulting-style master deck used as the structural and visual basis for PPTX exports.

Regenerate after layout changes:

```bash
node scripts/seed-pptx-template.mjs
```

Named shapes for automizer text replacement: `Title`, `Headline`, `Body`, `Callout`.
